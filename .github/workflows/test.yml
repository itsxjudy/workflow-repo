name: Workflow zero
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - id: step-zero
        name: workflow-test
        run: |
          echo "Hello Judy! $(date)"
      - id: step-one
        name: workflow-test-2
        run: |
          echo "Second Step"
